spring.application.name=distributed-rate-limiter

rate-limit.max-requests=5
rate-limit.window-size=60s
rate-limit.strategy=redis-sliding-window

# Disable PrometheusHttpServer in tests to avoid port-bind collisions across
# repeated ApplicationContext reloads (each @SpringBootTest creates a fresh context).
otel.prometheus.enabled=false
